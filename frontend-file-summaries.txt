{
  "frontend/src/App.tsx": "Main app router with all customer routes (including `/orders`, `/product/$id`) and admin routes (including `/admin/dashboard`, `/admin/users`); restores the full route tree that was accidentally trimmed in the previous build.",
  "frontend/src/pages/admin/AdminOrders.tsx": "Admin orders page redesigned as an inline card list — each order card shows Order ID, customer name, email, phone, full delivery address, payment badge, and a live fulfillment status dropdown without any modal click; clicking a card expands it to reveal ordered items with unit prices and totals, order notes, and Razorpay IDs.",
  "frontend/src/pages/ProductDetail.tsx": "Product detail page updated to pass the required guestDetails field (with empty strings) to createOrder when using the Buy Now flow, satisfying the new backend OrderInput interface.",
  "frontend/src/hooks/useQueries.ts": "Complete React Query hooks file with all hooks restored — products, orders, `useGetMyOrders`, `useCreateOrder`, site settings, `useWhatsappNumber` (with `useGetWhatsappNumber` alias), `useSetWhatsappNumber`, `useGetAllUsers`, `useIsStripeConfigured`, and `useCreateCheckoutSession`.",
  "frontend/src/pages/admin/AdminLogin.tsx": "Admin login page updated to redirect to `/admin/products` instead of the removed `/admin/dashboard` route after successful authentication.",
  "frontend/src/pages/admin/AdminProducts.tsx": "Admin products management page updated with 6 stats cards (total, visible, featured, outOfStock, launchingSoon, notVisible) and uses the new StatusBadge component in each product row for color-coded status display.",
  "frontend/src/components/ProductCard.tsx": "Product card component updated to include the required `guestDetails` field (with empty strings) in the `createOrder.mutateAsync` call for the Buy Now flow, fixing the TypeScript type error from the new `OrderInput` interface.",
  "frontend/src/pages/Checkout.tsx": "Checkout page that now dynamically fetches the Razorpay Key ID from the backend via useGetRazorpayKeyId instead of using a hardcoded key; shows an error if no key is configured and uses the fetched key to initialize the Razorpay SDK.",
  "frontend/src/components/admin/AdminAuthGuard.tsx": "Auth guard protecting admin routes that directly calls `isCallerAdmin()` (with `isAdmin()` fallback) on the actor once it's ready, with a 15-second timeout, proper error/denied/loading UI states, and retry/back-to-login actions.",
  "frontend/src/components/admin/AdminLayout.tsx": "Admin sidebar layout with \"Nature Glow\" brand name in the sidebar header, navigation links for Products/Orders/Settings, and sign-out button that clears auth and redirects to login.",
  "frontend/src/components/admin/ProductFormModal.tsx": "Full product create/edit modal updated to use all 5 new ProductStatus options (visible, outOfStock, launchingSoon, featured, notVisible) with a live StatusBadge preview; defaults to `ProductStatus.visible` for new products.",
  "frontend/src/components/admin/OrderDetailModal.tsx": "Order detail modal updated to display all guestDetails fields (fullName, email, phoneNumber, addressLine1, addressLine2, city, state, pincode, orderNotes) with fallback to shippingDetails; shows a Guest badge for anonymous orders, hides orderNotes section when empty, and includes a live fulfillment status dropdown.",
  "frontend/src/pages/admin/AdminSettings.tsx": "Admin settings page unchanged in logic; imports `useWhatsappNumber` and `useSetWhatsappNumber` which are now correctly exported from `useQueries.ts`.",
  "frontend/src/index.css": "Global styles with Playfair Display serif font and Lato sans-serif, Ayurvedic OKLCH color tokens, glassmorphism utilities, golden divider, scroll-triggered animation keyframes, and botanical shadow utilities.",
  "frontend/tailwind.config.js": "Tailwind config with all existing Ayurvedic brand tokens, fonts, shadows, and keyframes, plus the new `kenBurnsEffect` keyframe (translateX -5%→0% + scale 1.0→1.12) registered as the `kenburns` animation at 20s ease-in-out infinite alternate for a smooth cinematic slow-motion effect.",
  "frontend/src/utils/seedProducts.ts": "Seed products updated to use new ProductStatus values — `visible` instead of `active`, and `featured` for the sandalwood product to demonstrate the new featured status.",
  "frontend/index.html": "HTML entry point with updated Google Fonts import for Playfair Display and Lato, Razorpay script, and Nature Glow page title.",
  "frontend/src/components/Navbar.tsx": "Sticky navbar with scroll-aware shadow; adds an \"Orders\" link in the desktop nav and a Package icon button for mobile, both visible only when the user is authenticated.",
  "frontend/src/components/Footer.tsx": "Premium footer with deep forest green background, golden top divider, four-column layout (brand/social, quick links, products, contact), newsletter promo box, and caffeine.ai attribution.",
  "frontend/src/pages/Home.tsx": "Homepage with a static photorealistic hero section using the generated Ayurvedic facepack image as a full-width background, with gradient overlay, headline, CTA buttons navigating to `/shop`, trust badges, and scroll indicator, followed by FeaturedProducts, Benefits, Testimonials, and AboutAyurveda sections.",
  "frontend/src/components/MobileNavDrawer.tsx": "Mobile navigation drawer updated to include a \"My Orders\" link (with Package icon) that is only shown when the user is authenticated.",
  "frontend/src/hooks/useRazorpay.ts": "Updated Razorpay checkout hook that uses `isMobileDevice()` to build device-aware payment method configuration — desktop users see Cards → Net Banking → UPI QR (no collect/intent push), while mobile users get the native UPI intent app flow (Google Pay, Paytm, PhonePe) with cards and wallets.",
  "frontend/src/context/CartContext.tsx": "Cart context provider with addItem updated to accept an optional quantity parameter and no longer auto-opens the drawer (callers handle navigation to /cart instead).",
  "frontend/src/pages/Cart.tsx": "Cart page with two-step checkout (cart items → full checkout form), collects Full Name, Email, Phone, Street Address, Address Line 2 (optional), City, State, Pincode, and Order Notes (optional) for all customers including guests without requiring login; passes all fields as both shippingDetails and guestDetails to createOrder.",
  "frontend/src/components/FloatingWhatsApp.tsx": "Floating WhatsApp button with dual pulse rings, slide-up entrance animation, tooltip, backend number fetching with fallback, and pre-filled Ayurvedic message.",
  "frontend/src/components/Hero.tsx": "Full-screen hero section using the new spices/herbs flatlay image (`/assets/generated/hero-spices.dim_1920x1080.jpg`) as a Ken Burns animated background, with warm dark gradient overlays, \"Nature Glow\" brand name in Playfair Display gold text, animated golden underline, Ayurvedic tagline, description, Shop Now / Our Story CTAs, trust indicators, and scroll indicator.",
  "frontend/src/components/Benefits.tsx": "Benefits section with sage-green tinted background, six benefit cards (Natural, Chemical-Free, Handmade, Dermatologist Tested, Ancient Recipes, Cruelty-Free) with glassmorphism styling and scroll-triggered slide-up animations.",
  "frontend/src/components/Testimonials.tsx": "Testimonials section with six customer review cards on warm beige background, star ratings, product tags, author avatars, and trust stats row; all with scroll-triggered animations.",
  "frontend/src/components/AboutAyurveda.tsx": "Two-column About Ayurveda storytelling section with golden divider, serif headings, Ayurvedic philosophy copy, three doshas list, botanical image with floating card, and scroll-triggered slide-in animations.",
  "frontend/src/components/FeaturedProducts.tsx": "Featured products section updated to filter out notVisible products client-side as an extra safety guard before rendering ProductCards.",
  "frontend/src/components/AyurvedicHeroAnimation.tsx": "Full-width animated hero section (min-h-90vh) featuring a CSS-animated central mixing bowl with swirling herb contents, a stirring spoon animation, steam particles rising above the bowl, floating herb emoji particles swirling into the bowl, ingredient label cards on both left and right sides (Turmeric, Neem, Rose, Sandalwood, Multani, Honey), a headline with golden accent text, trust badges, a CTA button, and a scroll indicator — all using inline CSS keyframes and the existing Ayurvedic brand palette.",
  "frontend/src/hooks/useActor.ts": "Initializes the backend actor using the current Internet Identity; removed the non-existent `_initializeAccessControlWithSecret` call, added retry logic, and now returns `isError`, `error`, and `refetch` in addition to `actor` and `isFetching` so components can handle connection failures gracefully.",
  "frontend/src/components/admin/StatusBadge.tsx": "New reusable component that renders a color-coded badge for each of the 5 ProductStatus values (visible=green, outOfStock=red, launchingSoon=blue, featured=amber, notVisible=gray) with configurable size.",
  "frontend/src/pages/Shop.tsx": "Shop page updated to use new ProductStatus enum values in status filters (visible, featured, outOfStock, launchingSoon), with a client-side guard to filter out any notVisible products.",
  "frontend/src/pages/Orders.tsx": "New customer-facing orders page that fetches the authenticated user's orders via `useGetMyOrders`, displays each order with its ID (`#ORDER-XXXX`), date, total in INR, payment/fulfillment status badges, line items with product images and prices, and shipping address; shows skeleton loaders and an empty state.",
  "frontend/src/pages/PaymentSuccess.tsx": "Payment success page that invalidates the `myOrders` React Query cache on mount so the orders list is fresh; shows a success message with buttons to view orders or continue shopping.",
  "frontend/src/components/CartDrawer.tsx": "Cart drawer sheet component that redirects to `/cart` (instead of the removed `/checkout` route) when the user clicks \"Proceed to Checkout\".",
  "frontend/src/pages/PaymentFailure.tsx": "Payment failure page updated to navigate to `/cart` instead of the removed `/checkout` route for the \"Try Again\" button.",
  "frontend/src/utils/deviceDetection.ts": "New utility that exports `isMobileDevice()`, which returns true for mobile/tablet devices by combining user-agent pattern matching with a screen-width threshold (< 768px)."
}